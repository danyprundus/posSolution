@extends('app')@section('content')<div class="col-md-12 content-header" >    <h1><i class="fa fa-user"></i> {{trans('application.system_users')}}</h1></div><section class="content"><div class="row">    <div class="col-md-12">        <div class="box  box-primary">            @if(hasPermission('add_user'))                <div class="box-header with-border">                    <h3 class="box-title pull-right">                        <div class="box-tools ">                            <a href="{{ route('users.create') }}" class="btn btn-primary btn-xs pull-right" data-toggle="ajax-modal"> <i class="fa fa-user-plus"></i> {{trans('application.new_user')}}</a>                        </div>                    </h3>                </div>            @endif            <div class="box-body">                    <table class="table table-bordered table-striped table-hover datatable">                        <thead>                        <tr>                            <th></th>                            <th>{{trans('application.photo')}} </th>                            <th>{{trans('application.name')}} </th>                            <th>{{trans('application.username')}} </th>                            <th>{{trans('application.email')}} </th>                            <th>{{trans('application.phone')}} </th>                            <th>{{trans('application.role')}} </th>                            <th>{{trans('application.action')}} </th>                        </tr>                        </thead>                        <tbody>                        @foreach($users as $user)                        <tr>                            <td></td>                            <td><img src="{{ asset($user->photo != '' ? 'assets/img/uploads/'.$user->photo : 'assets/img/uploads/no-image.jpg' ) }}" class="listthumb img-rounded" width="36px" style="border-radius:6px"/></td>                            <td>{{ $user->name }} </td>                            <td>{{ $user->username }} </td>                            <td>{{ $user->email }} </td>                            <td>{{ $user->phone }} </td>                            <td>{{ $user->role->name }} </td>                            <td>                                @if(hasPermission('edit_user'))                                    {!! edit_btn('users.edit', $user->uuid) !!}                                @endif                                @if(Auth::user()->uuid != $user->uuid)                                   @if(hasPermission('delete_user'))                                    {!! delete_btn('users.destroy', $user->uuid) !!}                                   @endif                                @endif                            </td>                        </tr>                        @endforeach                        </tbody>                    </table>            </div>        </div>        <!-- End  Hover Rows  -->    </div></div></section>@endsection