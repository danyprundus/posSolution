@extends('app')@section('content')<div class="col-md-12 content-header" >    <h1><i class="fa fa-users"></i> {{ trans('application.clients') }}</h1></div><section class="content"><div class="row">    <div class="col-md-12">        <div class="box box-primary">            @if(hasPermission('add_client'))                <div class="box-header with-border">                    <h3 class="box-title pull-right">                    <div class="box-tools">                            <a href="{{ route('clients.create') }}" class="btn btn-primary btn-sm" data-toggle="ajax-modal"> <i class="fa fa-user-plus"></i> {{ trans('application.new_client') }}</a>                    </div>                    </h3>                </div>            @endif            <div class="box-body">                <table class="table table-hover table-bordered table-striped datatable">                    <thead>                    <tr>                        <th></th>                        <th>{{ trans('application.reference') }}</th>                        <th>{{ trans('application.name') }}</th>                        <th>{{ trans('application.email') }}</th>                        <th>{{ trans('application.phone') }}</th>                        <th>{{ trans('application.country') }}</th>                        <th>{{ trans('application.action') }}</th>                    </tr>                    </thead>                    <tbody>                    @foreach($clients as $client)                    <tr>                        <td></td>                        <td>{{ $client->client_no }}</td>                        <td>{{ $client->name }} </td>                        <td>{{ $client->email }} </td>                        <td>{{ $client->phone }} </td>                        <td>{{ $client->country }} </td>                        <td>                            {!! show_btn('clients.show', $client->uuid) !!}                            @if(hasPermission('edit_client')) {!! edit_btn('clients.edit', $client->uuid) !!} @endif                            @if(hasPermission('delete_client')) {!! delete_btn('clients.destroy', $client->uuid) !!} @endif                        </td>                    </tr>                    @endforeach                    </tbody>                </table>            </div>        </div>    </div></div></section>@endsection