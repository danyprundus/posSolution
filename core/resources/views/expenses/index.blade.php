@extends('app')@section('content')    <div class="col-md-12 content-header" >        <h1><i class="fa fa-credit-card"></i> {{trans('application.expenses')}}</h1>    </div><section class="content">    <div class="row">        <div class="col-md-12">            <div class="box  box-primary">                @if(hasPermission('add_expense'))                    <div class="box-header with-border">                        <h3 class="box-title pull-right">                            <div class="box-tools ">                                <a href="{{ route('expenses.create') }}" class="btn btn-primary btn-xs pull-right" data-toggle="ajax-modal"> <i class="fa fa-plus"></i> New Expense</a>                            </div>                        </h3>                    </div>                @endif                <div class="box-body">                    <table class="table table-bordered table-striped table-hover datatable">                        <thead>                        <tr>                            <th></th>                            <th>{{trans('application.name')}}</th>                            <th>{{trans('application.date')}}</th>                            <th>{{trans('application.category')}}</th>                            <th>{{trans('application.amount')}}</th>                            <th>{{trans('application.action')}}</th>                        </tr>                        </thead>                        <tbody>                        @foreach($expenses as $expense)                        <tr>                            <td></td>                            <td>{{ $expense->name }} </td>                            <td>{{ $expense->expense_date }} </td>                            <td>{{ $expense->category }} </td>                            <td>{{ format_amount($expense->amount) }} </td>                            <td>                                @if(hasPermission('edit_expense'))                                    {!! edit_btn('expenses.edit', $expense->uuid) !!}                                @endif                                @if(hasPermission('delete_expense'))                                    {!! delete_btn('expenses.destroy', $expense->uuid) !!}                                @endif                            </td>                        </tr>                        @endforeach                        </tbody>                    </table>                </div>            </div>        </div>    </div></section>@endsection