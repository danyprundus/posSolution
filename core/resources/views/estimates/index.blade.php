@extends('app')@section('content')<div class="col-md-12 content-header" >    <h1><i class="fa fa-quote-left"></i> {{trans('application.estimates')}}</h1></div><section class="content">    <div class="row">        <div class="col-md-12">            <div class="box  box-primary">                @if(hasPermission('edit_estimate'))                    <div class="box-header with-border">                        <h3 class="box-title pull-right">                            <div class="box-tools">                                <a href="{{ route('estimates.create') }}" class="btn btn-primary btn-xs pull-right"> <i class="fa fa-plus"></i> {{trans('application.new_estimate')}}</a>                            </div>                        </h3>                    </div>                @endif                <div class="box-body">                    <div class="table-responsive">                        <table class="table table-bordered table-striped table-hover datatable">                            <thead>                            <tr>                                <th></th>                                <th>{{trans('application.estimate_number')}}</th>                                <th>{{trans('application.client')}}</th>                                <th>{{trans('application.date')}}</th>                                <th class="text-right">{{trans('application.amount')}}</th>                                <th>{{trans('application.action')}} </th>                            </tr>                            </thead>                            <tbody>                            @foreach($estimates as $estimate)                            <tr>                                <td></td>                                <td>{{ $estimate->estimate_no }} </td>                                <td><a href="{{ route('clients.show', $estimate->client_id ) }}">{{ $estimate->client->name }}</a> </td>                                <td>{{ $estimate->estimate_date }} </td>                                <td class="text-right">{{ $estimate->currency.''.format_amount($estimate->totals['grandTotal']) }} </td>                                <td>                                    @if(hasPermission('view_estimate'))                                        <a href="{{route('estimates.show',$estimate->uuid)}}" data-rel="tooltip" data-placement="top" title="{{trans('application.view_estimate')}}" class="btn btn-xs btn-info"><i class="fa fa-eye"></i></a>                                    @endif                                        <a href="{{url('estimates/pdf',$estimate->uuid) }}" data-rel="tooltip" data-placement="top" title="{{trans('application.download_estimate')}}" class="btn btn-xs btn-primary"><i class="fa fa-download"></i></a>                                    @if(hasPermission('edit_estimate'))                                        <a href="{{route('estimates.edit',$estimate->uuid)}}" data-rel="tooltip" data-placement="top" title="{{trans('application.edit_estimate')}}" class="btn btn-xs btn-success"><i class="fa fa-pencil"></i></a>                                    @endif                                    @if(hasPermission('delete_estimate'))                                        {!! delete_btn('estimates.destroy', $estimate->uuid) !!}                                    @endif                                </td>                            </tr>                            @endforeach                            </tbody>                        </table>                    </div>                </div>            </div>        </div>    </div></section>@endsection