@extends('app')@section('content')    <div class="col-md-12 content-header" >        <h1><i class="fa fa-puzzle-piece"></i> {{trans('application.inventory')}}</h1>    </div>    <section class="content">        <div class="row">            <div class="col-md-12">                <div class="box  box-primary">                    <div class="box-body">                        <table class="table table-bordered table-striped table-hover datatable">                            <thead>                            <tr>                                <th></th>                                <th>{{trans('application.product')}}</th>                                <th>{{trans('application.category')}}</th>                                <th>{{trans('application.quantity')}}</th>                                <th></th>                            </tr>                            </thead>                            <tbody>                            @foreach($inventory as $inv)                                <tr>                                    <td></td>                                    <td>{{$inv->product->name}}</td>                                    <td>{{ $inv->product->categoryList->name }} </td>                                    <td>{{ $inv->sum('qty') }} </td>                                    <td>                                        @if(hasPermission('edit_product'))                                            {!! edit_btn('products.edit', $inv->product->uuid) !!}                                        @endif                                    </td>                                </tr>                            @endforeach                              </tbody>                        </table>                    </div>                </div>            </div>        </div>    </section>@endsection