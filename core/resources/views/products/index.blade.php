@extends('app')@section('content')<div class="col-md-12 content-header" >    <h1><i class="fa fa-puzzle-piece"></i> {{trans('application.products')}}</h1></div><section class="content">    <div class="row">        <div class="col-md-12">            <div class="box  box-primary">                @if(hasPermission('add_product'))                    <div class="box-header with-border">                        <h3 class="box-title pull-right">                            <div class="box-tools ">                                <a href="{{ route('products.create') }}" class="btn btn-primary btn-xs pull-right" data-toggle="ajax-modal"> <i class="fa fa-plus"></i> {{trans('application.new_product')}}</a>                            </div>                        </h3>                    </div>                @endif                <div class="box-body">                    <table class="table table-bordered table-striped table-hover datatable">                        <thead>                        <tr>                            <th></th>                            <th>{{trans('application.name')}}</th>                            <th>{{trans('application.code')}}</th>                            <th>{{trans('application.category')}}</th>                            <th>{{trans('application.price')}}</th>                            <th>{{trans('application.action')}} </th>                        </tr>                        </thead>                        <tbody>                        @foreach($products as $product)                        <tr>                            <td></td>                            <td>{{ $product->name }} </td>                            <td>{{ $product->code }} </td>                            <td>{{ $product->category }} </td>                            <td>{{ format_amount($product->price) }} </td>                            <td>                                @if(hasPermission('edit_product'))                                    {!! edit_btn('products.edit', $product->uuid) !!}                                @endif                                @if(hasPermission('delete_product'))                                    {!! delete_btn('products.destroy', $product->uuid) !!}                                @endif                            </td>                        </tr>                        @endforeach                        </tbody>                    </table>                </div>            </div>        </div>    </div></section>@endsection